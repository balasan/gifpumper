.activePages
  .activePageItem( bindonce ="page" ng-repeat="page in renderList")
      //- {{page}}
      .galleryImage
        a(thumbnail page-data="page.data" freeze w="500" h="500" bo-href="'/' + page.data.pageName | encode")
          img.thumb 
      .infoDiv